## refactoring code (required)

- Rename Dfs in DfsBinaryGroupFinder to Bfs (or redo code to reflect proper traversal)
- Replace VideoMetadata data with immutable data (convert to private variables instead of public)

## adding tests (required)

- Add tests for ExtractVideoData (maybe VideoSummaryApp?)
- Add tests for server folder (controllers, routers, services)

## improving error handling (required)

- Remove and replace generic Exceptions calls (throws and catch) with actual exceptions
- Add input validation for color values, threshold value, and empty arrays.

## writing documentation (required)

- Add documentation for video processing classes (ExtractVideoData, ExtractFrame, ProcessFrame, VideoSummaryApp)
- Add documentation for server folder (controllers, routers, services)

## improving performance (optional)

- Optional, may add later

## hardening security (optional)

- Optional, may add later

## bug fixes (optional)

- Optional, may add later

## other (optional)

- Optional, may add later